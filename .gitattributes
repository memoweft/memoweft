# Keep text files stable across operating systems.
* text=auto eol=lf
*.png -text
*.gif -text
*.jpg -text
*.jpeg -text
*.ico -text
*.db -text
*.gguf -text

# These snapshots are compared byte-for-byte in CI.
tests/api/api-surface.snapshot        text eol=lf
tests/prompts/prompt-hashes.snapshot  text eol=lf
