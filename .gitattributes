# 机读快照必须逐字比对 —— 钉死 LF。
#
# 背景（2026-07-10 实测）：仓库无 .gitattributes 且开发机 core.autocrlf=true 时，
# fresh clone 会把这些文本快照 smudge 成 CRLF（`git ls-files --eol` 显示 w/crlf），
# 而生成器用 '\n' 拼串 → `current === stored` 必然不成立 → api-freeze / 提示词哈希闸门
# 在 Windows 新克隆上【假红】。CI 跑 Linux 故一直没暴露。
#
# 这里只钉这两个机读快照，不做全仓 eol 归一（那会让 200+ 既有文件产生无关 diff）。
tests/api/api-surface.snapshot        text eol=lf
tests/prompts/prompt-hashes.snapshot  text eol=lf
