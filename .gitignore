# Secrets
.env
.env.local
.env.*.local
*.pem
*.key

# Local user data
*.db
*.db-journal
*.sqlite
dla.db
!tests/fixtures/*.db
testbench/*.db
testbench/seed-*.ps1
apps/*/data/
logs/
*.db-shm
*.db-wal

# Build output and dependencies
dist/
coverage/
*.tsbuildinfo
node_modules/
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Editors and operating systems
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/

# Local tooling and scratch space
mcp-publisher*
tmp/
temp/
scratch/
.doc-snippets-tmp/
.cache/
task-logs/
worklogs/
debug-output/
artifacts/
site/
*.mp4

# Benchmark datasets and generated run artifacts
bench/data/
bench/runs/

# ── AI 助手的本地工作流：只在本机存在，绝不进仓、绝不上远端 ──
#   此前这些只靠 .git/info/exclude 挡着，而那个文件是【本机私有】的：不进仓、不随克隆走。
#   换台机器、重新 clone、或换个 agent 环境，防护就完全消失。规则搬到这里才对所有环境生效。
.claude/
.codex/
.cursor/
.cursorrules
.windsurfrules
.aider*
.mcp.json
CLAUDE.md
CLAUDE.local.md
AGENTS.local.md
# 技能/工具默认往【不带点】的 output/ 写产物（文档生成物、个人文件），
#   一次 git add -A 就会写进 git 历史——推上去很难干净删除。
output/

# BMB 评测探针与跑分报告：人类已定【不公开】（报告里带模型名与端点信息）。
#   本地分支 probe/bmb-instrument-fix、probe/bmb-baseline 上它是已跟踪的，照常在本地提交
#   （.gitignore 不影响已跟踪文件）——这条规则挡的是【在 main 这条线上误加进来】。
probe/
