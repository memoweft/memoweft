{
  "name": "memoweft",
  "version": "0.0.0",
  "description": "MemoWeft — weave user evidence into a model-independent, traceable cognition layer. 套在大模型外部的用户认知层。",
  "keywords": ["llm", "agent", "memory", "cognition", "user-profile"],
  "license": "MIT",
  "engines": { "node": ">=24" },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kestercarroll702-gif/memoweft.git"
  },
  "bugs": {
    "url": "https://github.com/kestercarroll702-gif/memoweft/issues"
  },
  "homepage": "https://github.com/kestercarroll702-gif/memoweft#readme",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist/**/*.js", "dist/**/*.d.ts", "README.md", "LICENSE"],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test \"tests/**/*.test.ts\"",
    "testbench": "node testbench/server.mjs",
    "experience": "node testbench/server.mjs",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^24.0.0"
  },
  "_comment": "依赖刻意最小化（地图 cell 11）。存储 / HTTP / 日志均用 Node 内置（node:sqlite、node:http、node:fs）。新增依赖须有明确理由，默认拒绝。"
}
