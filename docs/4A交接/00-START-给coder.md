# 00 · 开始读我 —— 给接手"档2 · 上本地模型（验证）"的 coder

> 你是接手 MemoWeft 项目的 coder，这是**全新会话**。本文件夹（`docs/4A交接/`）是产品经理整理的交接。
> 先按顺序读文档进入状态，再动手。**别跳过。**
>
> 下文 `<仓库根>` = `DLA_rebuild`；`<本文件夹>` = `DLA_rebuild/docs/4A交接`。

## 读取顺序（务必按此序）

**A. 规则与现状（进项目必读）**
1. **`<仓库根>/AGENTS.md`** — AI 工作契约：铁律 + 工作流。**最先读。**
2. **`<仓库根>/STATE.md`** — 此刻状态 + 现有接口（尤其 `src/llm/pool.ts` 的 `llmPool` / `DLA_WRITE_LLM_*`、嵌入器在 11435）。

**B. 本次任务**

3. **`<本文件夹>/DLA_档2-上本地模型_给coder.md`** — **本次任务完整指引**：装 `qwen2.5:3b` + 配写路径模型 + 验证"本地能不能顶"。**这是你这轮的活儿。**

**C. 背景（理解"为什么先走这步"）**

4. **`<本文件夹>/DLA_4-A_交接_给coder.md`** §9 方向更新 — 档2 那"缠在一起的一坨"（本地模型 / 按授权路由 / 采集器 / distill 雷 / §6 折中）+「可切换模型」架构。**本步是这坨的钥匙第一步（只验证，不上路由）。**

**D. 已完成的前置**

5. `<本文件夹>/` 里 `DLA_修复_隐私开关接线`（问题1）、`DLA_4-A_档1_实现清单`（档1）、`DLA_治慢_落盘与写路径小模型`（治慢，含 `llmPool` 第一块）——都已完成。

**E. 按需查（别通读，守 context-economy skill）**
6. `<仓库根>/docs/项目地图.md` 相关 cell；`<仓库根>/LOG.md` 需追溯才查。

## 本次任务一句话

**上本地模型（验证版）**：装 `qwen2.5:3b`（ollama，连 11435）、配成写路径模型（`.env DLA_WRITE_LLM_*`），跑验收场景**对比"本地 3b vs 云端 mimo"的速度/质量/JSON 结构跟随**，验"本地能不能顶写路径"。**只做"装+配+验证"，不做按授权路由、不做采集器。**

## 红线（AGENTS.md + 任务清单）

- **只做这一步**（装+配+验证）：别顺手上"按授权路由"、别做采集器、别碰 distill 雷 / §6 折中。
- **6G 显存**：qwen2.5:3b 爆了就降 1.5b（如实记）。**本地质量大概率不如云端，如实报、别粉饰**（跟不上 JSON 结构就是关键证据）。
- `.env`、装模型你来；**"质量够不够顶"是主观验收、交所有者定**——你把客观数据（速度 / 报错 / JSON 跟随）摆清楚。
- 大概率不用改代码（`llmPool` 已支持 `DLA_WRITE_LLM_*`）；要改代码超预期、或方案与代码冲突 → **停下说一声**。
- 若动了代码：跑测试全绿、`docs-sync`（STATE.md + LOG.md）。

## 完成后

装配验证完，**把"本地 3b vs 云端"的对比数据 + 你的判断报给所有者**（够不够顶、要不要换更大的）——下一步（按授权路由 / 换模型）由所有者定。

---
*本文件由产品经理生成（2026-07-01）。任务详情见同文件夹《DLA_档2-上本地模型_给coder.md》。*
