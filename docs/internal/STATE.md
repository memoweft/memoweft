# MemoWeft · STATE（内部历史存档 · 非开工必读）

> ⚠️ **这不是 AI 开工入口。** 开工看仓库根的 `AGENTS.md` + `CURRENT.md` 就够了。
> 本文件是某个时间点的内部状态快照，归 `docs/internal/` 历史存档，**可能已过时**——只在查旧接口 / 旧决策的背景时翻，别默认通读。

**阶段**：阶段 0 ✅ · 1 画像+召回 ✅ · 阶段 2 纠正闭环 ✅ · 阶段 3 归因+主动询问 ✅ · **阶段 4-B 周期后台 ✅**（衰减/过期/召回门控/冲突复看/趋势）· 阶段 4-A 多源感知 · **档1 摄入口闭环 ✅** · **档2 真采集器 ✅**（Win32 前台窗口采集循环 + `npm run collector` 运行器）· **公开仓加固批次 ✅（2026-07-02）** · **架构归位·批次1 边界文档 ✅（2026-07-02）** · **架构归位·批次2 统一入口+受控管理API ✅（2026-07-02）** · **架构归位·批次3 收瘦入口+归档雪藏+testbench切受控API ✅（2026-07-02）** · **架构归位·批次4 Host迁移蓝图（设计文档）✅（2026-07-02）** · **架构归位·批次5 步0-6 Host 全完成 ✅** · **插件 v1 体验插件 ✅** · **架构归位·采集器迁插件包 ✅（2026-07-03）**（真采集迁 `plugins/collector-active-window/`＋Core 只留通用摄入口＋采集流接 Host `/api/observe`）· **npm 发布 memoweft@0.1.0 ✅（2026-07-04）** · **schema 版本化+迁移器 ✅（2026-07-04·随 0.2.0 发）**
**框架闭环**（总设计任务书）：**Phase 5-A 便携记忆包 ✅**（导入/导出/备份/恢复，保真）· **5-B 测试台导入导出 ✅**（`/api/export-bundle`、`/api/import-bundle?mode=dryRun|merge` + 备份/迁移面板）· **6-A 记忆管理页 ✅**（筛选/详情/改删/标失效/授权开关）· 6-B 图谱视图（**G1 后端 ✅** · G2 前端 ⬜）· **7-A Cloud Guard ✅**（trends/ask 路径补过滤）· **8-A 真采集器 ✅** · 9-A 星瑶最小宿主 · 10-A 插件契约 · 11-A 稳定性/迁移 · 12-A npm 发布
**最后更新**：2026-07-03
**架构归位**：三层边界（Core/Host/Plugin）已定稿——Core 管记忆怎么正确存在，Host 管使用和管理，Plugin 管扩展。边界与现状差距见 `boundaries.md`，实施路线见 `架构归位路线.md`。
**模型部署口径**：文档已改为 **Cloud-first onboarding**（开发者先用 OpenAI-compatible 云端端点跑通）+ **evidence 级授权**（`allowCloudRead` 控制哪些证据可进云端 prompt）+ **Hybrid/local 作为高级选项**。详见 `docs/deployment.md`。
**写路径**：原话 → 整理事件(distill) → 画像(consolidate) → 归因(attribute) → 索引(retriever.indexAll)　**读路径**：消息 → 召回相关认知 → 注入回话

## 当前可用接口（手上有什么 · 下个任务直接用，不必读实现）

- **统一入口（归位·批次2）** `src/core/` → `createMemoWeftCore({dbPath,llm?,embedder?,retriever?,config?,vectorDbPath?})→MemoWeftCore`：`ingestUserMessage`｜`ingestObservation`｜`recall`（共享召回门控，invalid/archived/越界/衰减全走）｜`handleConversationTurn`（同 conversationId 复用实例）｜`dropConversation`（丢会话实例·切人设/重开会话下句真重建·插件v1加）｜`updateProfile`｜`memory.*`（受控管理 7 操作 + `management_log` 审计表：invalidate/授权/removeSafely×2(删认知审计只存元数据不存原文·批次3)/merge(同subject,source标失效)/archive(archived_at,批次3全面雪藏:召回+写路径都跳过)/checkIntegrity；审计表可 clear=恢复出厂专用）｜**批次5步0新增** `memory.listEvidence/listCognitions/listEvents`（只读列取·带溯源+有效置信）｜`memory.resetSubject`（恢复出厂收口:清三层+审计+索引）｜`health()`（llmReady/embedReady·首启门用）｜`portable.*`｜`graph.*`｜`close`。无 .env 也能建（缺配降级）。Host 优先走这里，别散装拼 Store。
- **Schema 版本化 / 迁移器（0.2.0·2026-07-04）** `src/store/migrations.ts` → `runMigrations(db,{dbPath,fresh,migrations?,dryRun?,backup?,log?})`｜`getSchemaVersion(db)`｜`LATEST_SCHEMA_VERSION`｜`MIGRATIONS`（主入口也导出前三个）。全库版本号存 `PRAGMA user_version`。openStores 开库前判 `fresh`（文件不存在/:memory:）→ 新库盖最新版；老库（0.1.0=user_version 0）从版本号升，baseline(v1)对已有表 no-op、真改动从 v2 起。每条迁移独立事务（抛错整段回滚·user_version 事务内可回滚已实测）+ v2+ 迁移前自动 `.bak` 备份 + dry-run。**加新迁移约定：MIGRATIONS 追加一条 + 同步改对应 store 的 SCHEMA 常量（新库靠 store 建最新、老库靠迁移升），别再土 ALTER。** `tests/migrations.test.ts` 7 条（含"0.1.0 库无损升级数据不丢"）。
- **证据层** `src/evidence/store.ts` → `SqliteEvidenceStore(dbPath?)`
  - `put(input)→Evidence`（带 originId 幂等）｜ `get(id)` ｜ `all()` ｜ `byTimeRange(fromIso,toIso)` ｜ `update(id,{rawContent?,summary?,allowCloudRead?,allowInference?})`（6-A：授权位可改）｜ `remove(id)` ｜ `close()`
  - **隐私过滤** `src/evidence/privacy.ts` → `filterCloudReadable(items)`：写路径喂【云端】LLM 前筛掉 `allowCloudRead=false` 的证据；observed 默认不上云，作为 Cloud-guarded 的安全阀。
- **观察摄入（4-A 档1）** `src/perception/ingest.ts` → `ingestObservations(subjectId,observations,{evidenceStore,hostId?})→{stored,skipped}`：`Observation{kind,occurredAt,content,originId?,meta?,授权位?}` → `observed` 证据；授权 **显式 > `config.observedDefaults`**(local✓/cloud✗/infer✓)。骨架 `collectors/activeWindow.ts`：`activeWindowToObservation(sample)` + `ActiveWindowCollector` 契约。
- **认知层** `src/cognition/store.ts` → `SqliteCognitionStore`：`put` ｜ `get` ｜ `all(subjectId?)` ｜ `active(subjectId)`（批次3 语义=未失效**且未归档**，写路径雪藏靠它）｜ `sourcesOf(id)` ｜ `update(id,patch)` ｜ `remove(id)` ｜ `removeBySubject(id)` ｜ `close()`
- **事件层** `src/event/store.ts` → `SqliteEventStore`：`put` ｜ `get` ｜ `all(subjectId?)` ｜ `evidenceOf(id)` ｜ `coveredEvidenceIds(subjectId)` ｜ `remove` ｜ `removeBySubject` ｜ `close`
- **画像生成** `src/consolidation/updateProfile.ts` → `updateProfile(subjectId,{evidenceStore,eventStore,cognitionStore,retriever,llm,transaction?})`：distill→consolidate→attribute→index；返回 `timings` 与 `metrics{profileSize,promptChars}`（写路径规模观测，测试台已落盘），索引失败不回滚画像。
- **召回** `src/retrieval/`：`Retriever` 接口；`VectorRetriever(dbPath,embedder)`（indexAll 内部 hash 增量 diff，对外语义不变）/ `NullRetriever`；`OpenAICompatEmbedder` + `loadEmbedConfig()`（缺配返回 null）。召回门控：跳过失效认知 + 有效置信 < `minEffectiveConfidence`(80) 不注入。
- **归因 / 主动询问**：`attribute` 产低置信 hypothesis；`proposeAsk` 挑低置信假设；`revisitConflicts` 复看冲突认知。宿主决定是否开口和怎么问。
- **周期后台 4-B**：`effectiveConfidence`（读时衰减）、`expire`（临时类过期）、`aggregateTrends`（近 14 天 state 聚趋势）。
- **会话编排** `src/pipeline/conversation.ts` → `new Conversation({store,retriever,cognitionStore,llm,seedTurns?,systemPrompt?})`（`seedTurns`=打开旧会话把最近几轮种回窗口续聊；`systemPrompt`=宿主注入人设，缺省库内最朴素提示，cell 9 语气/角色归宿主），`handle(msg,opts)→TurnOutcome{reply,storedEvidence,recall,llmCalls,error}`。召回注入前按 subjectId 硬过滤兜底（契约见地图「召回边界」）。`RunLogger` 重开已存在会话 logger 时轮号接着历史往下。
- **LLM / env**：`loadLLMPool()` / `LLMPool.for('chat'|'write')`；先读 `MEMOWEFT_*`，回退 `DLA_*`。默认文档推荐云端 OpenAI-compatible；本地 Ollama/LM Studio 仍可作为 endpoint。
- **便携记忆包（Phase 5-A）** `src/portable/` → `exportBundle(subjectId,{evidenceStore,eventStore,cognitionStore},opts?)→MemoryBundle` ｜ `validateBundle(bundle)→{valid,errors,warnings}`（结构+引用完整性；致命 vs 软告警分级）｜ `importBundle(bundle,{...三 store,transaction?},{mode:'dryRun'|'merge'})→ImportPlan`。保真（保留原 id 与全部时间戳）、按 id/originId 幂等去重、非法包不写库、可选事务防污染。为此三个 store 各新增 `insert()`（按原 id 原样落库，导出的对偶）。不含向量索引（导入后 `retriever.indexAll` 重建）。
- **图谱视图（Phase 6-B · G1）** `src/graph/` → `buildMemoryGraph(subjectId,{evidenceStore,eventStore,cognitionStore},opts?)→MemoryGraphPayload{nodes,edges,stats}`。节点 subject/evidence/event/cognition；边 belongs_to_subject/distilled_into/supports/contradicts（conflicts_with/corrects 数据未存 → V1 不产，冲突/失效靠节点 credStatus/invalidAt 体现）。筛选 includeEvidence/includeInvalid/contentType/credStatus/sourceKind/onlyCloudBlocked/onlyConflicts/onlyHypotheses/q。**仅 payload；API `/api/memory-graph` + 前端力导向图（G2/G3）待接。**
- **采集插件（Collector Plugin · 归位 2026-07-03）** `plugins/collector-active-window/`（`@memoweft/collector-active-window` workspace）：真实采集**已整体迁出 Core**（`src/perception/collectors/` 已删）。`src/activeWindow.ts`（契约+样本→Observation 映射）·`src/activeWindowCollector.ts`（采集循环·自持缺省 `DEFAULT_SAMPLE_INTERVAL_SEC/MIN_DURATION_SEC`，不再依赖 Core config）·`src/win32Foreground.ts`（Win32 P/Invoke·双向 base64 防乱码·仅 Windows）。运行器 `run.mjs`（`npm run collector`）：采窗口→映射 generic Observation→`POST Host /api/observe`。数据流按路线 §3：**采集器 → Host 审核（采集开关 `MEMOWEFT_HOST_COLLECTOR` + 强制剥 `allowCloudRead` 保 observed 不上云）→ core.ingestObservation**。插件不直穿 Core/Store。测试 8 条（离线全注入）。
- **测试台** `testbench/`（**体验层 V3 应用壳**）：左侧栏（＋新会话 / 会话列表·点开续聊·hover 可归档 / 设置组[配置·记忆管理]+调试）+ 聊天正门（思考中动效 + 记忆气泡"记住了…"就地织进对话 + 右上「它记住我 N 件事」抽屉）+ 首启门（没配模型先向导）+ 调试区（原透视/47旋钮/证据库/整理记忆/猜测原因，平时收起）+ 备份迁移(5-B) + 记忆管理(6-A)。多会话后端：`/api/sessions`、`/api/session/open`(续聊)、`/api/session/archive`(软移除·加 .archived·数据不删)、`/api/health`。`npm run testbench` → `:7888`。
- **Host（归位·批次5 步0-6 · 全完成）** `apps/memoweft-host`（`@memoweft/host` workspace 包；根 workspaces `[".", "apps/*"]` + exports 单轨引 dist）：node:http 壳经 `import 'memoweft'` 调 Core，独立库 `data/host.db` + 聊天历史 `data/sessions/`，`npm start -w @memoweft/host` → `:7788`（避 Clash）。前端全 textContent 免 XSS、文案遵 naming。已搬：**步1 聊天**（`/api/chat`·单会话·后台画像更新调度器攒批防抖·回话失败不当正常回复）· **步2 配置向导**（`/api/gen-env` 拼 .env 不落盘 apiKey·`MEMOWEFT_EXPERIENCE_UI=off` 纯库模式）· **步3 记忆管理**（列认知/证据 + 标失效/改授权/删除·**全走 core.memory.***·把握度按有效把握度定档[confBand]·证据删除 blockers→force 两步）· **步4 多会话**（`/api/reset`/`/sessions`/`/session/{open,archive}`·续聊 seedTurns 重建·归档软移除可恢复·历史跟随库路径·侧栏会话列表）· **步5 备份恢复**（`/api/export-bundle`/`/import-bundle`(validateBundle+dryRun/merge)/`/factory-reset`(强确认+CSRF confirm 兜底)·全走 core.portable.*/resetSubject·「数据·备份」tab）· **步6 S0-S1 正门**（`/api/refresh` 立即整理[单飞]·`/api/cognition/count` 胶囊数·「它记住我 N 件事」胶囊+记忆抽屉·S1 新理解就地织进聊天流气泡[删加确认]）。**六步全完成**——Host 承接 testbench 全部用户功能，全走 core.* / 零 store 直穿 / 零 runtime 依赖。**采集入口（归位 2026-07-03）** `POST /api/observe`：收采集插件的 generic Observation → 审核（`COLLECTOR_ENABLED` 开关·off 回 403；`sanitizeObservation` 强制剥授权位保 observed 不上云）→ `core.ingestObservation`。前端无需入口（采集器直接 POST）。
- **插件 v1（2026-07-03）** `apps/memoweft-host/src/experiences/`（Host 内模块·零 Core 依赖）：`MemoWeftPlugin`={id,name,type:'experience',systemPrompt}（v1 只 systemPrompt·hooks/PluginContext 预留）· experience-plain（普通助手=原 REPLY_PERSONA）+ experience-xingyao（星瑶·守 naming §6 拟人但不过度承诺）· `GET /api/experiences`+`POST /api/experience` 切人设 · 顶栏选择器。切人设靠 **core.dropConversation** 丢旧实例真重建（Host activatedConvs 与 Core conversations Map 两套缓存一起清）+ 切人设第一句 seedTurns **只种用户话**（不认领旧人设回复、免被带跑；dogfood 修复）。证明 MemoWeft 通用框架、不是星瑶专用。

## 命令
`npm run typecheck` ｜ `npm test`（Core 144 过）｜ `npm run build` ｜ `npm test -w @memoweft/host`（Host 25 过）｜ `npm test -w @memoweft/collector-active-window`（采集插件 8 过）｜ `npm run testbench` ｜ `npm run collector`（采集器→Host :7788）

## 进行中任务断点
（无）
