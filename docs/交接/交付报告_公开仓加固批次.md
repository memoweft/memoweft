# 交付报告 · 公开仓加固批次

基线：main @ dec1c70，108 tests ｜ 交付分支：`chore/hardening-batch-202607`（已推送 origin）
建 PR 入口：https://github.com/kestercarroll702-gif/memoweft/pull/new/chore/hardening-batch-202607
（本机未装 gh，PR 需人点上面链接创建；下面全文可直接作 PR 描述粘贴）

## ✅ 完成

- [T1] 中英 README 静态 tests badge（写死 87 passing）换 CI workflow badge，正文测试数字全部去写死；Done/Not yet 区块以 STATE.md 为准重写（Done 10 条 / Not yet 6 条，两语言逐条对应）；reference/README.md 改写为"只读基线快照"说明（该文件原已存在，改写补全而非新建）。
- [T2] conversation.ts 召回循环加一行 subjectId 硬过滤。**红→绿留证**：先写测试、过滤行缺席时断言 1 精准失败（"他人 subject 的认知泄漏进了 recall"），加过滤行后转绿且阳性对照证明本人召回未误伤。
- [T3] embedder.ts fetch 加 `AbortSignal.timeout`，默认 60s，`MEMOWEFT_EMBED_TIMEOUT_MS`（兼容 `DLA_EMBED_TIMEOUT_MS`）可配；超时错误按 llm/client.ts 同款样式包装，走既有降级链。
- [T4] vectors 表加 hash 列（sha256），indexAll 内部增量 diff。计数测试 5 例全过：首建 N 条 embed 恰 N 条｜相同集合重建 embed 0 次｜改1删1增1 embed 恰 2 条且被删 id 不再命中｜空集合仍清空全表｜旧 schema 构造不抛错自动重建。
- [T5] `ConsolidateResult` 增 `profileSize`/`promptChars`（早退时为 0），`updateProfile` 透传 `metrics`，`ProfileUpdateRecord.summary` 增可选字段，**testbench server.mjs 落盘已接通**（runProfileUpdate 处）。新测试 3 例全过。
- [T6] 地图 cell 7 登记「召回边界 V1 契约」（含 B 方案升级前提）、cell 10 登记「11-A 债·写路径膨胀」（防重网先行 + dogfood 曲线触发）；STATE.md 更新（35 行，守 ≤40）；LOG.md 追加批次记录；docs-sync 检查单补 README/STATE 一致性一条。

6 个 commit 与任务一一对应（`git log main..HEAD`），无混杂改动。

## ⏭ 跳过（卡壳协议触发项）

- 无代码/文档子项跳过。唯一未竟事项：**PR 创建本身**——本机未装 gh，无法命令行建 PR。选项 A：人点上方链接手动建（推荐，30 秒）；选项 B：装 gh 后由 AI 代建。分支与报告均已就绪。

## 🔶 待人拍板

1. 首轮 CI 跑完后确认 badge 显示正常（badge 在 Actions 首次运行前可能显示 no status，属预期）。
2. dogfood 一段时间后看 profileSize/promptChars 曲线（logs/run-*.jsonl 的 profile_update 行），决定 11-A 写路径债的启动时机。
3. **合并后重启 testbench 提醒**：交付时你本机有个 testbench 正跑着旧代码（18:07 启动、占着 7888）。切到新代码首次启动时，VectorRetriever 会对 testbench-evidence.db 里的旧 vectors 表做一次「DROP 重建」迁移（设计如此：索引是可重建资产，下次画像更新自动回填），期间召回短暂为空，属预期、不丢证据/认知数据。冒烟前已备份两个 db 到会话 scratchpad 的 smoke-backup/。

## 集成验收记录（§6）

1. 全仓三绿：typecheck 退出 0 ｜ `npm test` **117 过 / 0 败**（108 基线零回归 + 9 新增：召回边界 1、索引增量 5、写路径仪表 3）｜ build 退出 0。
2. testbench 冒烟：因 7888 被本机在跑的旧服务占用，改用**隔离副本**冒烟（端口 7899、db/日志指向临时目录、代码即本分支）——启动正常、`/api/health` 返回 200 `{"llmReady":true,"embedReady":true}`，进程已关、临时文件已删、无残留；用户的服务与数据未被触碰。
3. §3 禁止清单逐条自查：`git diff main...HEAD --stat` 确认 ci.yml / package.json(engines) / config.ts / src/index.ts 零改动；reference/ 未删未挪（仅 README 文案，T1 授权范围）；dependencies 保持空；STATE.md 35 行；既有测试断言未改（retrieval.test.ts 仅 stub 加计数、断言语义不变）。
4. commit 对应检查：见上，6 commit 各归各任务。

## 三绿证据

```
> npm run typecheck   → tsc -p tsconfig.json         （退出码 0）
> npm test            → tests 117 / pass 117 / fail 0（基线 108 → 117）
> npm run build       → tsc -p tsconfig.build.json   （退出码 0）
Node v24.15.0（本地实测；CI 锁 Node 24，未动）
```
